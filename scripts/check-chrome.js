#!/usr/bin/env node
/**
 * Chrome Check Script (Deprecated)
 * 
 * This script was used to verify Chrome/Puppeteer installation for PDF generation.
 * Since PDF generation has been removed in favor of HTML-only reports, this script
 * is no longer needed but is kept for reference.
 */

console.log('🔍 Chrome Check Script')
console.log('⚠️  This script is deprecated - PDF generation has been removed')
console.log('✅ HTML report generation is working without Chrome/Puppeteer dependencies')
console.log('📄 Reports are now generated as downloadable HTML files')

// Exit successfully
process.exit(0)
